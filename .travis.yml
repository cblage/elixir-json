language: elixir
env: MIX_ENV=test
notifications:
  recipients:
    - carlos@carloslage.net
elixir:
  - 1.3
  - 1.4
  - 1.5
  - 1.6
script:
  - mix test
  - ./test/check_formatted.sh
after_script:
  - MIX_ENV=docs mix deps.get
  - MIX_ENV=docs mix inch.report
