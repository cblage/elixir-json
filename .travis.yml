language: elixir
env: MIX_ENV=test
notifications:
  recipients:
    - carlos@carloslage.net
elixir:
  - 1.3
  - 1.4
  - 1.5
  - 1.6
script:
  - MIX_ENV=test mix test
  - MIX_ENV=prod mix format --check-formatted
after_script:
  - MIX_ENV=docs mix deps.get
  - MIX_ENV=docs mix inch.report
